import { AperturaLabelPipe } from './apertura-label.pipe';

describe('AperturaLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AperturaLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
